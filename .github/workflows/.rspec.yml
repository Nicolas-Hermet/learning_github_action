name: "My-Awesome-Workflow"
on:
  pull_request:
    branches: [ "main" ]
    types: [ synchronize ]
jobs:
  fais-tourner-les-specs:
    uses: "./.github/workflows/.reusable.yml"
    - name: Run specs
      run: bundle exec rspec --fail-fast
